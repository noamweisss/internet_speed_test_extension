#!/usr/bin/env bash
# Runs scripts/check.sh on staged files. Bypassing with --no-verify is blocked for agents by .claude/settings.json.
exec "$(git rev-parse --show-toplevel)/scripts/check.sh" staged
