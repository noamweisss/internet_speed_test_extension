## What

One paragraph: what changed and why. Link the ADRs this PR relies on.

## Verified

- [ ] CI green (Windows build, Core tests, `scripts/check.sh all`)
- [ ] Unit tests added or updated for changed logic in `src/SpeedTest.Core`
- [ ] Manually tested on Windows in Command Palette (say what you did) — or state that it was not

## Safety impact

None: no change to what the extension can do (hosts, capabilities, dependencies, guards).
<!-- If docs/SAFETY-CONTRACT.md §2 files changed, replace the line above with a plain-language explanation and the ADR link. The safety-impact CI check enforces this (R13). -->

## Not verified / known gaps

List anything a reviewer should check by hand.

## Docs

- [ ] `CHANGELOG.md` updated
- [ ] `docs/SESSION-LOG.md` hand-off entry added
- [ ] `docs/PLAN.md` status updated
- [ ] New decisions recorded in `docs/decisions/`
